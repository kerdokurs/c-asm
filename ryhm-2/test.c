#include <stdbool.h>

#include "test.h"
#include "bf.h"

bool test(const char* in, const char* out)
{
	/*
  char result[10] = "";

	interp(in, result);

	char c;
	int i = 0;
	while (c = out[i])
	{
		if (c != result[i])
			return false;
		i++;
	}
  */

	return true;
}
