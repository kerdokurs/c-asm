#include <stdio.h>
#include <string.h>

#include "test.h"
#include "bf.h"

bool test(const char *in, const char *out)
{
  /* char result[80];
  interp(in, result);

  printf("%s\n", result);

  char c;
  int i = 0;
  while (c = out[i])
  {
    if (c != result[i])
      return false;
    i++;
  }
  */

  return true;
}
